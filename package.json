{
  "name": "electionmodel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "Oriol Castander",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.11",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}
